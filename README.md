# Illuminate Portal System

This repository now contains a complete, tested portal traversal system:

- Network modeling with nodes and directed/bi-directional portals.
- Player state (location, energy, keys).
- Turn-based cooldown management.
- Permission checks for required keys.
- Pathfinding across the portal graph.

## Quick start

```bash
python -m pip install -e .
pytest
```
